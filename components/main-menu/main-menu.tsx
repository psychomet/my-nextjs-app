import styles from "./main-menu.module.less";
import {Space, Menu} from "antd";
import {IMenu} from "@models/menu";

const {SubMenu} = Menu;


const MainMenu = ({menu}: { menu: IMenu[] }) => {
    return (
        <>


        </>
    );
};
export default MainMenu;
